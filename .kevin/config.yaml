project:
  name: "My Project"

agents:
  - name: "echo-agent"
    executor: "local"
    command: ["echo", "Agent running for task: {{.TaskID}} ({{.TaskTitle}}) at {{.TaskPath}}"]
    env_pass: ["USER"]
